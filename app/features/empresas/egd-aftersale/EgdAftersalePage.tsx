import { useRef } from "react";
import { Link } from "react-router";
import { useGSAP } from "@gsap/react";
import { gsap } from "../../../shared/lib/gsap.client";
const services = [["Protección", "Películas, recubrimientos y soluciones para conservar la apariencia."], ["Detallado", "Limpieza, restauración y cuidado estético con atención a los acabados."], ["Mantenimiento", "Diagnóstico y servicio para mantener cada unidad lista."]];
export function EgdAftersalePage() { const root = useRef<HTMLElement>(null); useGSAP(() => { if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return; gsap.from("[data-after-row]", { x: -32, opacity: 0, stagger: .12, duration: .7, scrollTrigger: { trigger: "[data-after-list]", start: "top 75%" } }); }, { scope: root }); return <main ref={root} className="bg-[#0c2447]"><section className="mx-auto max-w-[1400px] px-5 py-24 sm:px-8 sm:py-32"><img src="/egd-aftersale/logotipo-egd-aftersale.svg" alt="EGD Aftersale & Detailing Center" className="max-h-14 max-w-72 brightness-0 invert" /><h1 className="mt-12 max-w-5xl text-[clamp(2.6rem,5vw,5rem)] font-bold leading-[.96] tracking-[-.07em]">Cuidado técnico para que cada detalle se mantenga en su lugar.</h1></section><section data-after-list className="border-y border-white/15"><div className="mx-auto max-w-[1400px] px-5 py-12 sm:px-8">{services.map(([title, body], index) => <article data-after-row key={title} className="group grid gap-6 border-b border-white/15 py-10 last:border-0 md:grid-cols-[.7fr_1.3fr]"><p className="text-2xl font-bold tracking-[-.05em]">{title}</p><div><p className="max-w-xl leading-8 text-white/65">{body}</p><img src={`/egd-aftersale/imgs/${index + 4}.0.png`} alt="" className="mt-6 aspect-[4/5] w-full max-w-xs rounded-xl object-cover opacity-0 transition-all duration-500 group-hover:opacity-100" /></div></article>)}</div></section><section className="mx-auto max-w-[1400px] px-5 py-24 sm:px-8"><Link to="/contacto" className="rounded-full bg-white px-5 py-3 text-sm font-semibold text-[#0c2447]">Agenda una cita</Link></section></main>; }
